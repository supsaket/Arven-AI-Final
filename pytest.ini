[pytest]
# Restrict collection to real test files. Root scratch files matching the old
# "*_test.py" convention (e.g. _tmp_test.py, _tmp_test_reg.py) are excluded so
# they never break collection — they are kept in place, not deleted.
python_files = test_*.py
testpaths = tests
addopts = -q